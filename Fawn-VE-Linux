Task 1: What does the 3-letter acronym FTP stand for?
  Ez.
    man ftp

Task 2: Which port does the FTP service listen on usually?
  Ez, once again ed boi.
    If you don't remember, a quick nmap scan, google, or text book shows port 21

Task 3: FTP sends data in the clear, without any encryption. What acronym is used for a later protocol designed to provide similar functionality to FTP but securely, as an extension of the SSH protocol?
  This is rather easy due to knowing FTP is insecure. The secure counterpart is SFTP

Task 4: What is the command we can use to send an ICMP echo request to test our connection to the target?
  Once again, another easy thing. We can use ping (which is the answer), nmap -Pn <target_ip>

Task 5: From your scans, what version is FTP running on the target?
  This is easy, just either remember nmap commands or use man nmap
    nmap -sV <target_ip>

Task 6: From your scans, what OS type is running on the target?
  The nmap output explicity states the version right next to the service (however, it may not always be accurate in all cases)

Task 7: What is the command we need to run in order to display the 'ftp' client help menu?
  ftp -?

Task 8: What is username that is used over FTP when you want to log in without having an account?
  anonymous because... anonymous login.
    ftp <target_ip>
      anonymous
        enter
          successful login

Task 9: What is the response code we get for the FTP message 'Login successful'?
  230: it's generated right after a successful login

Task 10: There are a couple of commands we can use to list the files and directories available on the FTP server. One is dir. What is the other that is a common way to list files on a Linux system.
  ls is the correct command and common on a linux system

Task 11: What is the command used to download the file we found on the FTP server?
  get is the correct command. This can be discovered by using help.
    help <command> - outputs what the command is used for
  Once the file is downloaded, cat the file and the flag is shown.
